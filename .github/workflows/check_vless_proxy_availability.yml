name: Check VLESS proxy availability via Xray (base64 subscription)
on:
  schedule:
    - cron: '*/59 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y curl jq unzip python3 python3-pip

      - name: Download Xray
        run: |
          XRAY_VER=$(curl -s https://api.github.com/repos/XTLS/Xray-core/releases/latest | jq -r .tag_name)
          curl -L -o xray.zip "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VER}/Xray-linux-64.zip"
          unzip -o xray.zip xray geoip.dat geosite.dat
          chmod +x xray
          ls -la xray

      - name: Prepare files
        run: |
          mkdir -p githubmirror
          : > githubmirror/26_proxy_subscription_raw.txt
          : > githubmirror/26_proxy_subscription_base64.txt

      - name: Check VLESS via Xray
        run: |
          python3 << 'EOF'
          import subprocess, time, json, urllib.parse, os, socket
          from concurrent.futures import ThreadPoolExecutor, as_completed
          from threading import Lock
          from datetime import datetime, timezone

          XRAY_PATH = os.path.join(os.getcwd(), "xray")
          INPUT = 'githubmirror/26_ru_whitelist.txt'
          OUTPUT_RAW = 'githubmirror/26_proxy_subscription_raw.txt'
          
          # ✅ ТЕСТЫ 10s каждый
          IP_TEST_URL = 'https://api.ipify.org'
          HTTP_TEST_URL = 'http://example.com'
          HTTPS_TEST_URL = 'https://www.cloudflare.com/cdn-cgi/trace'

          MAX_WORKERS = 8
          MAX_TOTAL_TIME = 1800
          FUTURE_TIMEOUT = 40  # 12+10+10+10=42s

          process_lock = Lock()
          print_lock = Lock()
          active_processes = []

          def cleanup():
              with process_lock:
                  for p in active_processes[:]:
                      try:
                          p.terminate()
                          p.wait(timeout=1)
                      except:
                          try: p.kill()
                          except: pass
                      if p in active_processes:
                          active_processes.remove(p)

          def get_free_port():
              s = socket.socket()
              s.bind(('', 0))
              port = s.getsockname()[1]
              s.close()
              return port

          def wait_port(port, timeout=12):
              start = time.time()
              while time.time() - start < timeout:
                  try:
                      with socket.create_connection(("127.0.0.1", port), timeout=1):
                          return True
                  except:
                      time.sleep(0.2)
              return False

          def test_proxy_robust(port):
              """⭐ 3 теста по 10s: продолжаем если прошёл"""
              
              # 1. IP тест (10s)
              try:
                  ip = subprocess.check_output(
                      ['curl', '-s', '--max-time', '10', '-x', f'socks5h://127.0.0.1:{port}', IP_TEST_URL],
                      text=True, stderr=subprocess.DEVNULL, timeout=12
                  ).strip()
                  if not ip or len(ip) < 7: 
                      return False, None
                  print_via_lock(f"  ✅ IP: {ip}")
              except: 
                  return False, None

              # 2. HTTP example.com (10s) - продолжаем если прошёл
              try:
                  http_code = subprocess.check_output(
                      ['curl', '-s', '--max-time', '10', '-x', f'socks5h://127.0.0.1:{port}', 
                       '-o', '/dev/null', '-w', '%{http_code}', HTTP_TEST_URL],
                      text=True, stderr=subprocess.DEVNULL, timeout=12
                  ).strip()
                  if http_code != '200': 
                      return False, None
                  print_via_lock(f"  ✅ HTTP: {http_code}")
              except: 
                  return False, None

              # 3. HTTPS Cloudflare (10s) - продолжаем если прошёл
              try:
                  https_code = subprocess.check_output(
                      ['curl', '-s', '--max-time', '10', '-x', f'socks5h://127.0.0.1:{port}', 
                       '-o', '/dev/null', '-w', '%{http_code}', HTTPS_TEST_URL],
                      text=True, stderr=subprocess.DEVNULL, timeout=12
                  ).strip()
                  if https_code not in ['200', '403']: 
                      return False, None
                  print_via_lock(f"  ✅ HTTPS: {https_code}")
                  return True, ip
              except: 
                  return False, None

          def print_via_lock(msg):
              with print_lock:
                  print(msg)

          def check_one(link):
              port = get_free_port()
              u = urllib.parse.urlparse(link)
              q = urllib.parse.parse_qs(u.query)

              network = 'http' if q.get('type', ['tcp'])[0] == 'xhttp' else q.get('type', ['tcp'])[0]
              sec = q.get('security', ['none'])[0]
              sni = q.get('sni', [''])[0]
              fingerprint = q.get('fp', ['chrome'])[0]

              stream = {'network': network}
              if sec == 'reality' and 'pbk' in q:
                  stream.update({
                      'security': 'reality',
                      'realitySettings': {
                          'serverName': sni,
                          'publicKey': q['pbk'][0],
                          'shortId': q.get('sid', [''])[0],
                          'fingerprint': fingerprint
                      }
                  })
              elif sec == 'tls':
                  stream.update({
                      'security': 'tls',
                      'tlsSettings': {
                          'serverName': sni,
                          'allowInsecure': True,
                          'fingerprint': fingerprint
                      }
                  })

              cfg = {
                  'inbounds': [{
                      'listen': '127.0.0.1',
                      'port': port,
                      'protocol': 'socks',
                      'settings': {'udp': False}
                  }],
                  'outbounds': [{
                      'protocol': 'vless',
                      'settings': {
                          'vnext': [{
                              'address': u.hostname,
                              'port': u.port or 443,
                              'users': [{
                                  'id': u.username,
                                  'encryption': 'none',
                                  'flow': q.get('flow', [''])[0]
                              }]
                          }]
                      },
                      'streamSettings': stream
                  }]
              }

              cfg_file = f'cfg_{port}.json'
              with open(cfg_file, 'w') as f:
                  json.dump(cfg, f)

              p = subprocess.Popen([XRAY_PATH,'run','-c',cfg_file],
                                   stdout=subprocess.DEVNULL,
                                   stderr=subprocess.DEVNULL)

              with process_lock:
                  active_processes.append(p)

              result = None
              host = f"{u.hostname}:{u.port or 443}"

              if wait_port(port):
                  # ⭐ ПОСЛЕДОВАТЕЛЬНЫЕ ТЕСТЫ 10s каждый
                  success, ip = test_proxy_robust(port)
                  if success:
                      result = link
                      with print_lock:
                          print(f"⭐ {host} SUPER (IP+HTTP+HTTPS: {ip})")
                  else:
                      with print_lock:
                          print(f"✗ {host} test fail")
              else:
                  with print_lock:
                      print(f"✗ {host} timeout")

              try:
                  p.terminate(); p.wait(timeout=2)
              except:
                  try: p.kill()
                  except: pass

              with process_lock:
                  if p in active_processes:
                      active_processes.remove(p)

              try: os.remove(cfg_file)
              except: pass

              return result

          if not os.path.exists(INPUT):
              print("❌ INPUT not found")
              raise SystemExit(0)

          with open(INPUT) as f:
              links = [l.strip() for l in f if l.strip().startswith('vless://')]

          total_links = len(links)
          print(f"TOTAL: {total_links} | Tests: IP(10s)+HTTP(10s)+HTTPS(10s)")

          with open(OUTPUT_RAW, 'w') as f:
              f.write(f"# updated {datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M UTC')}\n")

          alive = []
          start = time.time()
          processed = 0

          with ThreadPoolExecutor(max_workers=MAX_WORKERS) as ex:
              futures = {ex.submit(check_one, l): l for l in links}
              
              for future in as_completed(futures):
                  try:
                      r = future.result(timeout=FUTURE_TIMEOUT)
                      if r:
                          alive.append(r)
                  except:
                      pass
                  
                  processed += 1
                  if processed % 10 == 0:
                      with print_lock:
                          print(f"📊 {processed}/{total_links} ({len(alive)} SUPER)")
                  
                  if time.time() - start > MAX_TOTAL_TIME:
                      print("⏰ GLOBAL TIMEOUT")
                      break

          cleanup()

          with open(OUTPUT_RAW,'a') as f:
              for l in alive:
                  f.write(l+"\n")

          elapsed = time.time() - start
          print(f"\n{'='*60}")
          print(f"⭐ SUPER PROXIES | Total: {total_links} | Alive: {len(alive)} | Time: {elapsed:.1f}s")
          print(f"📁 Raw file: {len(open(OUTPUT_RAW).readlines())} lines")
          print(f"{'='*60}")
          EOF

      - name: Encode Base64
        run: |
          RAW="githubmirror/26_proxy_subscription_raw.txt"
          B64="githubmirror/26_proxy_subscription_base64.txt"
          if [ $(wc -l < "$RAW") -gt 1 ]; then
            tail -n +2 "$RAW" | base64 -w 0 > "$B64"
            COUNT=$(tail -n +2 "$RAW" | wc -l)
            echo "✅ SUPER proxies: $COUNT (IP+HTTP+HTTPS 10s each)"
          else
            : > "$B64"
            echo "⚠️ No SUPER proxies"
          fi
          ls -la githubmirror/*.txt

      - name: Commit & Push
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          RAW="githubmirror/26_proxy_subscription_raw.txt"
          if [ $(wc -l < "$RAW") -gt 1 ]; then
            git config user.name "github-actions"
            git config user.email "github-actions@github.com"
            git add githubmirror/*.txt
            COUNT=$(tail -n +2 "$RAW" | wc -l)
            git diff --cached --quiet || git commit -m "⭐ Update $COUNT SUPER proxies [$(date +'%Y-%m-%d %H:%M UTC')]"
            git pull --rebase origin main || true
            git push "https://x-access-token:${GH_PAT}@github.com/${GITHUB_REPOSITORY}" HEAD:main
            echo "✅ Pushed $COUNT SUPER proxies"
          else
            echo "⚠️ No SUPER proxies to commit"
          fi
