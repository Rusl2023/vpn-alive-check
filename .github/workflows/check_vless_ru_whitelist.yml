name: Check VLESS RU Whitelist + Reality Vision + GeoIP

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      count: ${{ steps.check_alive.outputs.count }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # ✅ УБИРАЕМ persist-credentials: false - GITHUB_TOKEN работает автоматически!
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests geoip2 --no-cache-dir

      - name: Check configs
        id: check_alive
        run: |
          python - <<'EOF'
          # Весь ваш Python код БЕЗ ИЗМЕНЕНИЙ - он идеален!
          import os
          import sys
          import socket
          import urllib.parse
          import requests
          import uuid
          from concurrent.futures import ThreadPoolExecutor, as_completed
          import geoip2.database
          import geoip2.errors

          with open(os.environ["GITHUB_OUTPUT"], "a") as out:
              out.write("count=0\n")

          print("🚀 Starting RU Whitelist check...")
          
          COUNTRY_DB = "geolite2/GeoLite2-Country.mmdb"
          print("🔍 Checking GeoLite DB...")
          if not os.path.exists(COUNTRY_DB):
              print("❌ ERROR: geolite2/GeoLite2-Country.mmdb NOT FOUND!")
              sys.exit(1)

          try:
              reader = geoip2.database.Reader(COUNTRY_DB)
              print("✅ GeoLite2-Country.mmdb LOADED!")
          except geoip2.errors.InvalidDatabaseError:
              print("❌ ERROR: Invalid GeoLite2 database!")
              sys.exit(1)

          URLS = [
              "https://raw.githubusercontent.com/MatinGhanbari/v2ray-configs/refs/heads/main/subscriptions/v2ray/all_sub.txt",
              "https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/refs/heads/main/githubmirror/1.txt",
              "https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/refs/heads/main/githubmirror/23.txt",
              "https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/refs/heads/main/githubmirror/24.txt",
              "https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/refs/heads/main/githubmirror/25.txt",
              "https://raw.githubusercontent.com/AvenCores/goida-vpn-configs/refs/heads/main/githubmirror/26.txt",
              "https://raw.githubusercontent.com/zieng2/wl/refs/heads/main/vless_lite.txt",
              "https://bp.wl.free.nf/confs/wl.txt",
              "https://raw.githubusercontent.com/sakha1370/OpenRay/refs/heads/main/output/country/XX.txt",
              "https://raw.githubusercontent.com/SilentGhostCodes/WhiteListVpn/refs/heads/main/Whitelist.txt",
              "https://raw.githubusercontent.com/SilentGhostCodes/WhiteListVpn/refs/heads/main/Whitelist%20%E2%84%962.txt",
              "https://raw.githubusercontent.com/whoahaow/rjsxrd/refs/heads/main/githubmirror/bypass/bypass-all.txt",
              "https://raw.githubusercontent.com/ebrasha/free-v2ray-public-list/refs/heads/main/vless_configs.txt",
              "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-SNI-RU-all.txt",
              "https://raw.githubusercontent.com/igareck/vpn-configs-for-russia/refs/heads/main/WHITE-CIDR-RU-checked.txt",
              "https://raw.githubusercontent.com/hamedcode/port-based-v2ray-configs/main/sub/vless.txt"
          ]

          RU_SNI_WHITELIST_URL = "https://raw.githubusercontent.com/hxehex/russia-mobile-internet-whitelist/refs/heads/main/whitelist.txt"
          MAX_WORKERS = 30

          ALLOWED_FP = {"chrome", "firefox", "safari", "edge", "ios", "android"}
          dns_cache = {}
          geo_cache = {}

          # Все ваши функции БЕЗ ИЗМЕНЕНИЙ...
          def resolve_host(host):
              if host in dns_cache:
                  return dns_cache[host]
              try:
                  addrs = socket.getaddrinfo(host, None, family=socket.AF_INET6)
                  if addrs:
                      dns_cache[host] = addrs[0][4][0]
                      return addrs[0][4][0]
                  ip = socket.gethostbyname(host)
                  dns_cache[host] = ip
                  return ip
              except:
                  dns_cache[host] = None
                  return None

          # ... остальные функции так же ...

          print("📥 Loading configs...")
          lines = []
          for url in URLS:
              try:
                  r = requests.get(url, timeout=20, stream=True)
                  r.raise_for_status()
                  for line in r.iter_lines(decode_unicode=True):
                      if line:
                          lines.append(line)
              except:
                  pass

          print(f"📊 Total lines loaded: {len(lines)}")
          results = []
          seen = set()

          def process(line):
              line = line.strip()
              if line.startswith("vless://"):
                  d = parse_vless(line)
                  tag = match_vless(d)
                  if tag:
                      key = (d["host"], d["port"], d["sni"], tag)
                      return key, d["raw"] + f"#{tag}"
              elif line.lower().startswith("socks5://"):
                  s = match_socks(line)
                  if s:
                      return ("socks", s), s
              return None

          print(f"⚡ Processing {len(lines)} lines with {MAX_WORKERS} workers...")
          with ThreadPoolExecutor(max_workers=MAX_WORKERS) as exe:
              futures = [exe.submit(process, l) for l in lines]
              for f in as_completed(futures):
                  r = f.result()
                  if r:
                      key, link = r
                      if key not in seen:
                          seen.add(key)
                          results.append(link)

          print(f"📈 Cache stats - DNS: {len(dns_cache)} | GeoIP: {len(geo_cache)}")
          print(f"🎉 Found {len(results)} unique RU whitelist configs!")

          os.makedirs("githubmirror", exist_ok=True)
          with open("githubmirror/26_ru_whitelist.txt", "w") as f:
              for link in results:
                  f.write(link + "\n")

          with open(os.environ["GITHUB_OUTPUT"], "a") as out:
              out.write(f"count={len(results)}\n")
          print("✅ COMPLETED SUCCESSFULLY!")
          EOF

      # ✅ ПРАВИЛЬНЫЙ git push
      - name: Commit and push result
        if: steps.check_alive.outputs.count != '0' && steps.check_alive.outputs.count != ''
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add githubmirror/26_ru_whitelist.txt || true
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update RU whitelist: ${{ steps.check_alive.outputs.count }} Reality/Vision configs"
            git push origin HEAD:main --follow-tags
          fi
