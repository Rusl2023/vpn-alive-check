name: Check VLESS Servers

on:
  schedule:
    - cron: '*/9 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip requests

      - name: Check VLESS servers (TCP avg latency ≤ 450ms)
        id: check_alive
        run: |
          python - <<'EOF'
          import os
          import time
          import socket
          import requests
          from concurrent.futures import ThreadPoolExecutor, as_completed

          URL = "https://github.com/AvenCores/goida-vpn-configs/raw/refs/heads/main/githubmirror/26.txt"

          TIMEOUT = 0.8
          MAX_LATENCY = 450
          CHECK_COUNT = 3
          MAX_WORKERS = 120

          print("Downloading 26.txt ...")
          lines = requests.get(URL, timeout=20).text.splitlines()
          vless_list = [l.strip() for l in lines if l.strip().startswith("vless://")]

          print(f"Total VLESS links: {len(vless_list)}")

          def tcp_ping(host, port):
              start = time.time()
              sock = socket.create_connection((host, port), timeout=TIMEOUT)
              sock.close()
              return int((time.time() - start) * 1000)

          def check_vless(link):
              try:
                  hp = link.split('@', 1)[1].split('?', 1)[0]
                  host, port = hp.split(':', 1)
                  port = int(port)

                  pings = []
                  for _ in range(CHECK_COUNT):
                      ping = tcp_ping(host, port)
                      if ping > MAX_LATENCY:
                          return None
                      pings.append(ping)

                  avg_ping = sum(pings) // len(pings)
                  if avg_ping <= MAX_LATENCY:
                      return avg_ping, link

              except Exception:
                  return None

          results = []
          start_all = time.time()

          with ThreadPoolExecutor(max_workers=MAX_WORKERS) as exe:
              futures = [exe.submit(check_vless, v) for v in vless_list]
              for i, f in enumerate(as_completed(futures), 1):
                  res = f.result()
                  if res:
                      avg_ping, link = res
                      results.append((avg_ping, link))
                      print(f"[OK] {avg_ping} ms")

                  if i % 100 == 0:
                      print(f"Checked {i}/{len(vless_list)}")

          results.sort(key=lambda x: x[0])

          elapsed = int(time.time() - start_all)
          print(f"Finished in {elapsed} sec. Alive: {len(results)}")

          os.makedirs("githubmirror", exist_ok=True)
          with open("githubmirror/26_alive.txt", "w") as f:
              for avg_ping, link in results:
                  f.write(link + "\n")

          with open(os.environ["GITHUB_OUTPUT"], "a") as out:
              out.write(f"count={len(results)}\n")
          EOF

      - name: Commit and push result
        if: steps.check_alive.outputs.count != '0'
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add githubmirror/26_alive.txt
          git diff --cached --quiet || git commit -m "Update alive VLESS servers"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }} HEAD:main
